import { GestureActionName } from './gesture_action_name';

export class ScrollToBottomGestureActionName implements GestureActionName {
    public getJapaneseName(): string {
        return 'ページ末尾へ';
    }
} 
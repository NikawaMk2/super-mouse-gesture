import { GestureActionName } from './gesture_action_name';

export class ScrollToTopGestureActionName implements GestureActionName {
    public getJapaneseName(): string {
        return 'ページトップへ';
    }
} 
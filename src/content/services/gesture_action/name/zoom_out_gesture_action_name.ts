import { GestureActionName } from './gesture_action_name';

export class ZoomOutGestureActionName implements GestureActionName {
    public getJapaneseName(): string {
        return 'ズームアウト';
    }
} 
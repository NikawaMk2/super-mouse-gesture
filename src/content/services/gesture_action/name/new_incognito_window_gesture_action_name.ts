import { GestureActionName } from './gesture_action_name';

export class NewIncognitoWindowGestureActionName implements GestureActionName {
    public getJapaneseName(): string {
        return '新しいプライベートウィンドウを開く';
    }
} 
import { GestureActionName } from './gesture_action_name';

export class ShowFindBarGestureActionName implements GestureActionName {
    public getJapaneseName(): string {
        return '検索バー表示';
    }
} 